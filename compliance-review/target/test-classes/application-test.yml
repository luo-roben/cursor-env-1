spring:
  datasource:
    url: jdbc:mysql://localhost:3306/compliance_review_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: compliance
    password: compliance123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  sql:
    init:
      mode: never

compliance:
  llm:
    default-model: mock
    temperature: 0.0
    max-retries: 1
  prompt:
    review-template: |
      Mock review template for testing.
      {lawArticles}
      {customRules}
      {contentType}
      {productType}
      {content}
